head
    title Sensor test page
    meta(charset="UTF-8")
    meta(name="viewport", content="initial-scale=1.0, maximum-scale=1.0, width=device-width")
    link(rel="stylesheet" href="./fader/style.css")
    // tone
    script(src="https://cdnjs.cloudflare.com/ajax/libs/tone/14.7.77/Tone.js")
    script(src="./fader/main.js")
        
body(onload='init()')
    #result
    #slider-container 

    svg#slider-bg(viewbox='0 0 1200 600' width='100%' height='100%' style='position:absolute; top:0px; left:0px;')
        defs
            include fader/fader_gradient
            include fader/fader_style

        include fader/fader_faders.pug
    canvas#cover.touch_pickup_ignore.prevent-select

include fader/fader_gen
include fader/fader_action
include note-player/toolbar
script.
    // this page is based on the fader demo but should be oriented towards quick sensor testing for the fixed mappings of boards we have been using...
    let params = new URLSearchParams(window.location.search);
    if(params.has('board')){
        switch(params.get('board')){
            case 'tb':
                console.log('touch board')
            break;
            case 'btb':
                console.log('btb board')
            break; 
        }
    }

    // it's been a while since I used the screen instrument class, so I'm just going to emulate it for this demo
    let instrument = {
        setNotes(notes){
            // well, that was easy!
            notes.forEach((noteName,i)=>{
                if(sines[i]){
                    sines[i].frequency.rampTo(noteName, 1);
                }
            })
        }
    }

    // we'll need to remove presets for now, too
    document.querySelectorAll('#toolbar__preset').forEach(x=>x.remove())
    